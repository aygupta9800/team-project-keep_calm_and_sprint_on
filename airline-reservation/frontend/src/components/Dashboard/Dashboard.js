import React from 'react';
// import { useSelector } from 'react-redux';
// import ApplicationCustomerNavbar from '../ApplicationCustomerNavbar/ApplicationEmployeeNavbar.js';
// import ApplicationEmployeeNavbar from '../ApplicationEmployeeNavbar/ApplicationEmployeeNavbar.js';


// this page is rendered after successfull signin or login
const Dashboard = () => {
//   const loginDetails = useSelector((state) => state.login.user);
//   const signInDetails = useSelector((state) => state.signIn.user);
//   const userType = loginDetails.userType || signInDetails.userType;
  return (
    <div>
      {/* <ApplicationCustomerNavbar  />  */}
      {/* Give ternary condition here, to render which application navbar based on the type of login/signup */}
    </div>
  );
};

export default Dashboard;